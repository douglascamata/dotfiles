# Ignore the `external_files` folder, where I put files that are managed
# externally to chezmoi (i.e. LazyVim's lazy-lock.json)
external_files/
external_files/**/*

{{- if ne .chezmoi.hostname "roci" }}
# hyprland stuff
.config/hypr
.config/waybar
.local/bin/scratchpad

# audio stack set up
.config/pipewire
.config/wireplumber
{{- end }}

